# 다음 세션에서 바로 시작할 것 (2026-09-21 갱신)

> 다른 PC 로 옮겨서 이어할 때 **이 파일부터 읽는다.**
>
> 🔴 이 파일은 **오래되면 거짓말이 된다.** 2026-09-03 버전은 "첫 할 일 = ⭐찜 시안 고르기"로
> 11일간 멈춰 있었는데 정작 찜·알림은 그 다음 날들에 다 적용돼 있었다(작업110·111).
> 세션을 끝낼 때 여기부터 맞출 것.

## 🔴 첫 할 일 — 네이버 서치어드바이저 캡처 받기 (2026-09-21 이후)

아시안게임 페이지를 하루에 13장 새로 열었다(종목 12 + 중계 1). **실제로 노출을 먹는지는
네이버 「콘텐츠 노출/클릭」 화면에서만 보인다** — Claude 크롬 확장은 `*.naver.com` 을 통째로
막아서 내가 직접 못 본다. 화니에게 그 화면(검색 키워드 TOP 30 1~3페이지 + 검색 웹문서)을
캡처로 받아 아래를 판정한다.

| 볼 것 | 2026-09-21 기준선 |
|---|---|
| `아시안게임*` 쿼리가 TOP 30 에 들어오는지 | 0건 (그날 전까지 페이지가 없었다) |
| `오늘 야구 해설` 클릭 | 162클릭 · CTR 20.3% (사이트 1위 쿼리) |
| 전체 | 클릭 3.8천 · 노출 487.7만 · CTR 0.1% |

검색량은 `npm run keywords -- "키워드"` 로 직접 잴 수 있다(검색광고 API, 무료).

## 그 다음 — Vercel 대시보드 Usage
2026-09-16(작업124)에 `revalidate` 를 전부 배포 주기(21600)에 맞췄다. **판정은 코드가 아니라
대시보드다.** `npm run usage` 는 Hobby 라 402 로 멈춘다 — 사람이 화면을 봐야 한다.

| 항목 | 기준선(9/14 실측, 최근 30일) | 기대 |
|---|---|---|
| ISR Writes | **244,795 / 200,000** | **4~5만대**로 내려가야 한다 |
| Fluid Active CPU | 8h33m / 4h | 같이 내려간다 |
| Fast Origin Transfer | 10 / 10 GB | 소폭 감소 |

🔴 **안 내려갔으면 전제가 틀린 것이다.** 그때는 `revalidate` 를 더 만지지 말고 **무엇이 쓰는지**
부터 다시 볼 것(라우트별 ISR Writes). 페이지 라우트는 헤더로 못 잰다 —
`X-Vercel-Cache: PRERENDER` 만 보이고 `revalidate` 값은 안 나온다(`rss.xml` 같은 route
handler 만 `s-maxage` 가 보인다).

🔴 **같은 화면에서 매치 robots 재판정(작업126)도 같이 판정된다.** ISR·CPU 가 **올라갔으면
`/match/` 를 다시 막는다** — 절차는 `public/robots.txt` 재판정 주석.

## 지금 막 끝난 것 (2026-09-16, 작업124·125·126)

세 개 다 **라이브 배포·실측 완료**(push `9c085e82..57003f7f` · CI 성공 · deploy run 35058639270).

### 작업124 — revalidate 예산을 배포 주기에 맞춤

- 원인이 이미 코드 주석에 있었다 — 데이터가 배포 번들 안이라 재생성해도 **같은 HTML**.
  623페이지가 전부 `3600` 이었고 이론상 월 44.8만 회.
- 날짜 의존 9곳 → `21600` · 날짜 무관 1곳 → `false`. `rss.xml`·`llms-full.txt` 는 1800 이었다.
- **매치 OG 아카이브 렌더 제거** — 라이브 확인: 현재 매치 `200 image/png 145KB`,
  아카이브 매치 `308 → og-default.png`, 아카이브 매치 페이지 `200`(404 안 남).
- 정책 정본 = `src/app/page.tsx` 「revalidate 정책 정본」 주석. 가드 `test:revalidate-budget`.

### 작업125 — 선수 페이지 `/player/[slug]` 12장

- 라이브 확인: `/player/이정후` 200 · noindex 없음 · 팀 링크 있음 · FAQ 4문항 ·
  사이트맵에 player 12 / match 0.
- 🔴 **손흥민·이강인은 예정 경기가 0 이다**(MLS 는 Apple TV+, 라리가 편성 2건). 리드가
  "앞으로 편성된 중계는 없습니다" 로 나간다. 사실이지만 **최다 검색 이름의 페이지가 가장 얇다.**
- 🔴 되돌리지 말 것: 순위·홈원정·플랫폼 분해는 팀 페이지 몫 / 팀 페이지 「코리안리거」 칩이
  유일한 유입 링크 / 로스터 10일 낡으면 페이지가 사라지는 게 설계.

### 작업126 — `/match/` 를 네이버·AI 봇에만 열었다

- 라이브 robots 전수 파싱: **차단 4**(`*`·Googlebot·bingbot·Daumoa) / **허용 22**(Yeti·GPTBot 포함).
- 사이트맵·IndexNow 는 계속 제외.
- 🔴 **전제가 확인 전이다** — 9/07 AI 브리핑 인용은 **차단된 상태에서** 난 것이다. 여는 것으로
  얻는 건 AI 인용이 아니라 네이버 웹문서 랭킹이라는 별개 레인이다.

## 그 전에 끝난 것 (2026-09-16 오전, 작업123 — 브랜드 엔티티 정의문)

- 원인은 인지도가 아니라 **이름의 자기설명력**. `한해설` 은 `한해`+`설` 로 쪼개 읽혀 앵커가 못 된다.
- 🔴 **진짜 결함은 엇갈림이었다** — 정의문이 스키마 없는 `/about` 산문에만 있고 `FAQPage` 가
  도는 `/faq` 엔 정의 질문이 없었다. **"정의문이 있다" ≠ "추출된다".**
- 🔴 **`/about` h1 을 `한해설~` 로 올리지 말 것**(구글이 브랜드 쿼리에 홈 대신 이 페이지를 올린 이력).
- 🔴 **남은 건 코드 밖 — 제3자 언급 0.** disquiet.io·커뮤니티 소개 1~3건, SNS bio 를
  `한해설(haeseol.com)` 로 고정. **나무위키 자기 문서는 역효과라 하지 말 것.**

## 그 전에 끝난 것 (2026-09-15, 작업120~122)

- 첫 방문 안내 모달 · 유령 구독 3건 정리 · 찜 해제가 서버까지 가는 길 셋 차단 ·
  카드 뱃지 자리(모바일 하단) · 홈 「내 팀」 흰 판 · `design:audit` 오탐 둘 수정.
- 🔴 `design:audit` 의 `tap` 문턱 24px 를 올리지 말 것(44px 로 올리면 칩·뱃지 744건).
- 상세는 `docs/worklog.md` 작업120·121·122.

## 날짜가 박힌 할 일

| 언제 | 무엇 |
|---|---|
| 9/19 | 아시안게임 개막. ①홈 배너가 **메달 집계(시안 C)로 바뀌었는지** 라이브 확인(날짜 분기라 로컬로만 봤다) ②개인 종목(수영·양궁·펜싱·태권도)이 네이버 `koreaPlayer` 에 채워졌는지 |
| 9/21 | `kbo-draft-2026-broadcast` 를 **실제 지명 결과**로 갱신 |
| 9/21~ | (아시안게임 13장 관측은 위 첫 할 일) AEO 재측정 — 절차 `docs/seo-aeo-baseline-2026-09.md`. 기준선 네이버 AI 브리핑 인용 1/6. **이번엔 매치 robots 를 연 뒤 첫 측정이다** |
| 9/28 전 | 토스 가격 자동 갱신이 실제로 돌았는지(`generated/toss-refresh.log`). 안 돌면 그날부터 가격이 화면에서 사라진다 |
| 9/30 이후 | **글감 큐가 비어 있다.** 9/23·24·25·29 까지만 고정 |
| 10/05 뒤 | 아시안게임 폐막. `/asian-games` 를 남길지 301 로 흡수할지 결정 |
| 2~4주 | **Vercel 대시보드**(위 첫 할 일) · 선수 페이지 노출(기준 0) · 9/14 SEO 배포분 재측정(`오늘 축구 중계`·`프로배구 중계`·`프로농구 중계`) |
| 10월 초 | 브랜드 엔티티 재측정 — ChatGPT·Perplexity 에 `한해설이 뭐야` / `haeseol.com 뭐야`. 기준선 = 9/16 현재 둘 다 환각 |

## 지켜보는 것 (작업은 아님)

- **모달이 계속 구독을 만드는지** — `gh workflow run push-notify.yml -f dry=true` → `subscribers`.
  9/15 기준 **2건**. 며칠 2 에서 멈추면 문구 문제가 아니라 **유입이 인앱 웹뷰라 구독이 안 걸리는 것**
  (유입 81%가 네이버 앱). 그때는 모달을 더 다듬지 말고 기대치를 낮춰 잡을 것.
- **토스 클릭이 잡히는지** — 쉐어링크 실적 대시보드. 자리별로 나눠 보려면 `subTag` 가 필요하다
  (지금은 같은 상품=같은 링크라 홈/매치가 안 갈린다).
- 찜이 재방문으로 이어지는지(GA4) · 애드핏 적립금이 월 500원에서 움직이는지.

## 화니 결정 대기

- **PC 인트로가 6.1초다**(모달은 그 뒤 0초에 뜬다). 더 빨리 보여주려면 인트로를 줄여야 하는데
  화니가 정한 연출이라 손대지 않았다 — 3초대까지 가능.
- **가이드 글 SEO 개선안 6개** — auto-memory `project-guide-seo-verdict`.

## 사람 몫으로 오래 대기 중

- **틱톡 수동 업로드 실험** — 두 달 넘게 대기. 이게 막혀서 틱톡 코드를 못 건드린다.
- **빙 웹마스터 › 사이트맵 잉여 3개 삭제** — 정본은 `https://haeseol.com/sitemap.xml` 하나.
- **쿠팡파트너스 대시보드 2026-04-15~07-30 실적 확인** — 같은 트래픽으로 3.5개월 돌린
  실데이터인데 아직 아무도 안 봤다. 추정할 필요가 없다.

## 다른 PC 로 옮길 때 챙길 것

- `.env` 는 동기화가 안 된다. 토스 키는 OneDrive 공용 파일에 있어 그대로 읽힌다
  (`npm run secrets`). **VAPID·인스타·네이버 키는 아직 `.env` 에만 있다** —
  필요하면 `npm run secrets:push -- --key=<키이름>`.
- 🔴 **토스 호출은 PC 마다 IP 를 등록해야 한다**(업체당 10개). `npm run toss:check` 가 찍어 준다.
- 토스 가격 갱신 작업 스케줄러는 PC 마다 따로 등록(`tools/toss-refresh-daily.ps1`).
