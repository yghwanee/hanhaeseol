# 다음 세션에서 바로 시작할 것 (2026-09-15 저녁 갱신)

> 다른 PC 로 옮겨서 이어할 때 **이 파일부터 읽는다.**
>
> 🔴 이 파일은 **오래되면 거짓말이 된다.** 직전 버전(2026-09-03)은 "첫 할 일 = ⭐찜 시안
> 고르기"로 11일간 멈춰 있었는데, 정작 찜·알림은 그 다음 날들에 다 적용돼 있었다
> (작업110·111). 세션을 끝낼 때 여기부터 맞출 것.

## 🔴 첫 할 일 — 모달이 계속 구독을 만드는지 본다

`gh workflow run push-notify.yml -f dry=true` → `subscribers`.
**2026-09-15 기준 2건**(화니 것 1 + 모달로 켠 1 = 그날 16:38 첫 전환). 이 숫자가 늘어나는지가
모달의 유일한 판정 기준이다.

며칠 2 에서 멈추면 문구·자리 문제가 아니라 **유입이 인앱 웹뷰라 구독 자체가 안 걸리는 것**
(유입 81%가 네이버 앱 — 거기선 원래 안 된다). 그때는 모달을 더 다듬지 말고 「별 눌러보기」
쪽으로 기대치를 낮춰 잡을 것.

**화니 결정 대기 하나**: PC 인트로가 6.1초다(모달은 그 뒤 0초에 뜬다). 더 빨리 보여주려면
인트로를 줄여야 하는데 화니가 정한 연출이라 손대지 않았다 — 3초대까지 가능.

## 지금 막 끝난 것 (2026-09-15 저녁, 작업122 — 화니 라이브 피드백 6건)

전부 **화면을 보고 나온 지적**이라 눈으로 확인할 것이 남아 있다. 라이브 배포 완료
(`gh workflow run deploy.yml`, run 34957039121).

- 모달: 켜기 성공이 「오늘 하루」를 접던 것 제거 — **켰다 끈 사람에게 안내가 다시 뜬다.**
- 모달: 실패 본문을 **문장 배열**로(문장 중간 줄바꿈 제거) · 문구 `break-keep` ·
  모바일 각주 제거 · 딤을 `h-[100dvh]` + 대칭 `py` 로 **정중앙**(실측 3폭) ·
  못 켜는 환경 CTA = **「경기 추가하기」** + PC 처럼 가장 임박한 경기로 데려간다.
- 카드: 모바일만 `[플랫폼][해설]` 을 **맨 아래 가운데(`mt-6`)**, 상태(LIVE·종료)는 상단 유지.
- 홈 「내 팀 다음 경기」 = **흰 판**(`.w-invert-surface`, 토큰 반전).
- 카드 순서 = 최근전적 → 뱃지 → **하이라이트(맨 아래)**. 「오늘의 편성」 제목 글자 삭제
  (`sr-only` 로 태그만 — `#schedule` 앵커·제목 구조 때문).
- `design:audit` 오탐 둘 수정(`clip` 의 sr-only, `tap` 의 `::after`). 🔴 **`tap` 문턱 24px 를
  올리지 말 것** — 44px 로 올리면 칩·뱃지 744건이 뜬다.

최종 검증(같은 날): `design:audit` **10개 항목 0건**(4폭 × 15경로) · CI 가드 58개 · 빌드 623페이지.

🔴 **확인할 것**: ①폰에서 카드 아래 뱃지 줄이 최근전적과 충분히 떨어져 보이는지
②흰 판이 스크롤 중에 과하지 않은지(판이 둘 이상 흰색이 되면 강조가 죽는다)
③모달 정중앙이 실기기 사파리에서도 맞는지(`100dvh` 로 쟀다).

## 그 전에 끝난 것 (2026-09-15, 작업121)

- 유령 구독 3건 삭제. 찜 해제가 서버까지 가는 길의 끊긴 자리 셋을 막았다 — state 게이트 제거 ·
  떠날 때 `sendBeacon`(`/api/push/follows` 신설) · 30일 수명 상한 + 주 1회 하트비트.
- 푸터 알림 토글 제거(컨트롤은 홈 「내 팀」 섹션 하나) · **찜하면 알림 기본 켜짐**.
  🔴 푸터 버튼이 찜 동기화를 겸했으므로 구독 조작을 `lib/push/client.ts` 로 빼고 UI 없는
  `PushFollowsSync` 가 전역에서 동기화를 맡는다 — 다시 버튼에 묶지 말 것.
- ✅ 실기기 2단계 확인 완료. 상세는 worklog 121.
- **첫 방문 안내 모달**(시안 10개 → 화니가 1번 선택, 레퍼런스 쿠폰 모달 형태) — 홈에만.
  🔴 찜 0개 + 알림 켜짐이면 「내 팀」 자리에 끄기 한 줄을 남긴다(안 그러면 끌 방법이 사라진다).
  🔴 「닫기」는 기억하지 않는다 — 홈에 들어올 때마다 다시 뜨고, 차단은 「오늘 하루 보지 않기」뿐.
  🔴 실패(시크릿·차단)는 조용히 닫지 않고 이유를 말한다. 켠 뒤 안내는 3.4초 머문다.

## 그 전에 끝난 것 (2026-09-15, 작업120)

- 인트로 엠블럼 즉시 표시(하이드레이션 대기 제거) · 필터/검색/카드 팀명↔전적 간격 · 날짜 탭 테두리 · 모바일 전적 뱃지 축소.
- 스코어 누락: 결과 룩업 한쪽 일치 안전망 + 전적 감사(`audit:aliases`) — alias 20건(결과 7 · 전적 13).
- 푸시 구독 주소 교체 유령 방지 + dry 진단·remove 입력.
- 아시안게임 배너 교체(개막 전 시안 B · 개막일부터 시안 C 메달 집계) · 클릭 → 허브 `#today`(TODAY 표시·테두리).

## 그 전에 끝난 것 (2026-09-14 저녁, 디자인 후속 — 작업119)

- 서브헤더 제거(GNB 하나만) · GNB 오른쪽 정렬 · 해설 통계 메뉴 제외.
- 해설 뱃지 한국어(흰 채움)/현지(검은 박스)/종료(글자만) · 카드 상단 한 줄에 [플랫폼][해설][종료],
  폰에선 리그명만 …처리 · 하단 줄은 하이라이트 있을 때만.
- 테두리 박스 겹침 10곳 수정(`-my-2` 탭 영역 핵이 원인).
- 홈 히어로 문구 교체 · 「한국어 해설만」 버튼 수정(같은 경로 Link 라 필터가 안 켜졌다).
- 모바일: 히어로 숨김 · 인트로 정지 화면. 후원 배너 파랑. 순위 광고를 리그 탭 아래로.
- 디자인 전체 검수(같은 날 밤): 카드 윗줄 세로 정렬 · 뱃지 24px · **사이드 배너 1440px 부터**(1280 에서 본문을 덮었다) ·
  거터 `px-5 sm:px-6` 통일 · 리그 페이지 1100px 2열 · 색 토큰 위반 정리. 검수 방법은 worklog 119 마지막 항목.
  🔴 `design:audit` 는 행 정렬·뱃지 높이·고정 요소 겹침·거터를 안 본다 — 보강 후보.
- ✅ **실기기 확인 완료(2026-09-14, 화니 폰).** 인트로·카드 윗줄·히어로. 로컬 Playwright 실측(19경로×4폭 레이아웃,
  기능 40건, 6폭 디자인 검수)과 함께 끝났다.

## 그 전에 끝난 것 (2026-09-14, 작업117)

- **토스쇼핑 쉐어링크 라이브** — 지면 4곳(홈 상단·홈 인라인·매치·가이드 1편), 상품 2개.
- **자격증명 PC 3대 공유** — OneDrive `hwanee solutions\_secrets\hwanee.env` 가 정본.
  다른 PC 는 그냥 돌리면 되지만, **토스 호출만 그 PC IP 를 추가 등록**해야 한다
  (`npm run toss:check` 가 등록할 IP 를 찍어 준다. 업체당 10개).
- **가격 매일 자동 갱신** — 이 PC 작업 스케줄러 `한해설 토스 가격 갱신`(매일 09:30).
  🔴 **이 PC 에만 등록돼 있다.** 다른 PC 를 주력으로 쓰게 되면 거기에도 걸어야 한다
  (`tools/toss-refresh-daily.ps1`). 안 돌아도 14일 뒤 가격만 숨겨지고 카드는 남는다.
- 아시안게임 홈 배너 세로 축소(100→72px).

## 날짜가 박힌 할 일

| 언제 | 무엇 |
|---|---|
| 9/19 | 아시안게임 개막. ①홈 배너가 **메달 집계(시안 C)로 바뀌었는지** 라이브 확인(날짜 분기라 로컬 미리보기로만 봤다) ②개인 종목(수영·양궁·펜싱·태권도)이 네이버 `koreaPlayer` 에 채워졌는지 — 지금 116건은 단체 위주다 |
| 9/21 | `kbo-draft-2026-broadcast` 를 **실제 지명 결과**로 갱신(9/14 발행분) |
| 9/21 | AEO 재측정 — 절차는 `docs/seo-aeo-baseline-2026-09.md`. 첫 측정은 네이버 AI 브리핑 인용 1/6 |
| 9/28 전 | 토스 가격 자동 갱신이 실제로 돌았는지 한 번 확인(`generated/toss-refresh.log`). 안 돌았으면 그날부터 가격이 화면에서 사라진다 |
| 9/30 이후 | **글감 큐가 비어 있다.** 9/23·24·25·29 까지만 고정돼 있다 |
| 10/05 뒤 | 아시안게임 폐막. `/asian-games` 허브를 남길지 301 로 흡수할지 결정 |
| 2~4주 뒤 | 9/14 SEO 배포분 재측정(`오늘 축구 중계`·`프로배구 중계`·`프로농구 중계` 등) |

## 지켜보는 것 (작업은 아님)

- **토스 클릭이 잡히는지** — 쉐어링크 실적 대시보드. 1~2주 뒤. 이게 유일한 판정 기준이다.
  자리별로 나눠 보려면 subTag 를 붙여야 한다(지금은 같은 상품=같은 링크라 홈/매치가 안 갈린다).
- 찜이 재방문으로 이어지는지(GA4) · 애드핏 적립금이 월 500원 수준에서 움직이는지.

## 사람 몫으로 오래 대기 중

- **틱톡 수동 업로드 실험** — 두 달 넘게 대기. 이게 막혀서 틱톡 코드를 못 건드린다.
- **빙 웹마스터 › 사이트맵 잉여 3개 삭제** — 정본은 `https://haeseol.com/sitemap.xml` 하나.

## 다른 PC 로 옮길 때 챙길 것

- `.env` 는 동기화가 안 된다. 다만 토스 키는 OneDrive 공용 파일에 있어 그대로 읽힌다
  (`npm run secrets` 로 확인). **VAPID·인스타·네이버 키는 아직 `.env` 에만 있다** —
  필요하면 `npm run secrets:push -- --key=<키이름>` 으로 같이 올릴 것.
- 토스 가격 갱신 스케줄러는 PC 마다 따로 등록해야 한다(위 참조).
